using UnityEngine;
using Object = System.Object;

//namespace Tests;

/*
public static class Assertions
{
    private const string FAIL =     "\n[xxxxxxxxxx]  FAILURE! : ";
    private const string SUCCESS =  "\n[..........]  SUCCESS! : ";
    
    public static void AreEqual(Object expected, Object actual)
    {
        Debug.Log(BaseMessage(expected.Equals(actual), expected.ToString(), actual.ToString()));
    }

    private static string BaseMessage(bool success, string expected, string actual)
    {
        string header = success ? SUCCESS : FAIL;
        return $"{header}Expected {expected} but got {actual}";
    }
}
*/